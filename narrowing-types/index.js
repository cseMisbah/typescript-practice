/*
function printTodos(todos: string[] | string | null) {
  if (todos) {
    console.log(todos);
  } else {
    console.log("no todo");
  }
}

const todos = ["todo1", "todo2"];
printTodos(todos);
*/
var firstName;
var lastName;
lastName = 23;
